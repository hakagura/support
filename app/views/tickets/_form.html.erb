<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resumo %></br>
    <%= f.text_field :summary %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :cols => 80, :rows => 10 %>
  </div>

  <div class="field">
    <%= f.label :requested_by %><br />
    <%= f.text_field :requested_by %>
  </div>

  <div class="field">
    <%= f.label :priority %><br />
		<%= f.radio_button :priority, 0%>Urgente!<br/>
		<%= f.radio_button :priority, 1%>Alta<br/>
		<%= f.radio_button :priority, 2%>Media<br/>
		<%= f.radio_button :priority, 3%>Baixa
  </div>
  <div class="actions">
	<%= f.submit "Gravar" %> 
  </div>
<% end %>
